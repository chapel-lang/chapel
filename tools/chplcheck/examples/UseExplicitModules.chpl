/*
  Code should use explicit module declarations rather than relying on auto-inserted implicit modules.
*/

writeln("Hello, World!"); 

module myModule {
    proc main() {
        writeln("Hello, World!");
    }
}