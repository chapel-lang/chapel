/*
    Modules should use PascalCase naming convention.
*/

module modX {
  var x: string = "Module MX";
  proc printX() {
    writeln(x);
  }
}

module ModX {
  var x: string = "Module MX";
  proc printX() {
    writeln(x);
  }
}