LAUNCHER_SRCS = \
        chpl_cfg_launch.c \

SVN_SRCS = $(LAUNCHER_SRCS)
SRCS = $(SVN_SRCS)

RUNTIME_CFLAGS += $(SUPPORT_SETENV_CFLAGS) -D_CHPL_HOME="$(CURDIR)/$(CHAPEL_ROOT)" -D_PVM_ROOT="$(CURDIR)/$(PVM_ROOT)"

LAUNCHER_OBJS = \
	$(LAUNCHER_SRCS:%.c=$(LAUNCH_OBJDIR)/%.o)

LAUNCHER_INCLS += -I$(PVM_ROOT)/include
