MEM_SRCS = chplmem.c

SVN_SRCS = $(MEM_SRCS)
SRCS = $(SVN_SRCS)

MEM_COMMON_OBJS = $(MEM_SRCS:%.c=$(MEM_OBJDIR)/%.o)
MEM_LAUNCHER_OBJS = $(MEM_SRCS:%.c=$(MEM_LAUNCHER_OBJ_DIR)/%.o)

RUNTIME_CFLAGS += -I$(MEM_HEAPMM_INCLUDE_DIR)

$(MEM_HEAPMM_LIB_DIR)/libheapmm.a:
	cd $(THIRD_PARTY_DIR) && $(MAKE) libheapmm
