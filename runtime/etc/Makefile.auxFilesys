ifneq (,$(findstring curl,$(CHPL_MAKE_AUXFS)))
	GEN_LFLAGS += \
		$(CHPL_AUXIO_INCLUDE) \
		$(CHPL_AUXIO_LIBS)
	LIBS += -lcurl
endif 

ifneq (,$(findstring hdfs,$(CHPL_MAKE_AUXFS)))
	GEN_LFLAGS += \
		$(CHPL_AUXIO_INCLUDE) \
		$(CHPL_AUXIO_LIBS)
	LIBS += -lhdfs -ljvm
endif 
