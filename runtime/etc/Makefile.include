
include $(CHPLRTDIR)/../make/Makefile.platform
include $(CHPLRTDIR)/etc/Makefile.macros

all:
	gcc $(CFLAGS) -o $(BINNAME) -I $(CHPLRTDIR)/include -L $(CHPLRTDIR)/src/$(PLATFORM) $(CHPLRTDIR)/src/$(PLATFORM)/main.o $(CHPLSRC) -lchpl -lm $(LIBS)

