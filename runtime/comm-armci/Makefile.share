COMM_SRCS = \
	chpl_cfg_comm.c \

SVN_SRCS = $(COMM_SRCS)
SRCS = $(SVN_SRCS)

COMM_OBJS = \
	$(COMM_SRCS:%.c=$(COMM_OBJDIR)/%.o)

ARMCI_INSTALL = $(RUNTIME_ROOT)/../third-party/armci/install/$(CHPL_MAKE_PLATFORM)

INCL_CFLAGS += -I$(ARMCI_INSTALL)/include -I$(MPI_INCLUDE)

RUNTIME_CFLAGS = $(INCL_CFLAGS) -DARMCI_ENABLE_GPC_CALLS
