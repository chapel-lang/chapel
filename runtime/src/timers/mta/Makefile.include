TIMERS_SUBDIR = src/timers/$(CHPL_MAKE_TIMERS)

TIMERS_OBJDIR = $(RUNTIME_ROOT)/$(TIMERS_SUBDIR)/$(RUNTIME_OBJDIR)

ALL_SRCS += $(CURDIR)/$(TIMERS_SUBDIR)/*.c

include $(RUNTIME_ROOT)/$(TIMERS_SUBDIR)/Makefile.share
