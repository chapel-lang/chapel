MEM_SRCS = mem-dlmalloc.c

SVN_SRCS = $(MEM_SRCS)
SRCS = $(SVN_SRCS)

MEM_DEFINES = USE_LOCKS -DONLY_MSPACES -DMSPACES
MEM_COMMON_OBJS = $(MEM_SRCS:%.c=$(MEM_OBJDIR)/%.o)

RUNTIME_CFLAGS += -I$(DLMALLOC_INCLUDE_DIR) -D$(MEM_DEFINES)

