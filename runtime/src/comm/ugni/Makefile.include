COMM_SUBDIR = src/comm/ugni

COMM_OBJDIR = $(RUNTIME_BUILD)/$(COMM_SUBDIR)
COMM_LAUNCHER_OBJDIR = $(LAUNCHER_BUILD)/$(COMM_SUBDIR)

ALL_SRCS += $(CURDIR)/$(COMM_SUBDIR)/*.c

include $(RUNTIME_ROOT)/$(COMM_SUBDIR)/Makefile.share
