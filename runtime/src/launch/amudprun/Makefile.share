LAUNCHER_SRCS = \
        chpl_cfg_launch.c \

#
# is this the right time to set this variable?  Or is user code
# compile time
#
RUNTIME_CFLAGS += -DLAUNCH_PATH="$(CURDIR)/$(CHAPEL_ROOT)/third-party/gasnet/$(GASNET_INSTALL_SUBDIR)/bin/"

SVN_SRCS = $(LAUNCHER_SRCS)
SRCS = $(SVN_SRCS)

LAUNCHER_OBJS = \
	$(LAUNCHER_SRCS:%.c=$(LAUNCH_OBJDIR)/%.o)
