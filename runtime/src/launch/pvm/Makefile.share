LAUNCHER_SRCS = \
        launch-pvm.c \

SVN_SRCS = $(LAUNCHER_SRCS)
SRCS = $(SVN_SRCS)

RUNTIME_CFLAGS += $(SUPPORT_SETENV_CFLAGS) -DCHPL_HOME=\"$(CURDIR)/$(CHAPEL_ROOT)\" -DPVM_ROOT=\"$(CURDIR)/$(PVM_ROOT)\"

LAUNCHER_OBJS = \
	$(LAUNCHER_SRCS:%.c=$(LAUNCH_OBJDIR)/%.o)

LAUNCHER_INCLS += -I$(PVM_ROOT)/include
