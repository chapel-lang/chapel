#!/usr/bin/env perl

use File::Basename;

$preset_stm=$ENV{'CHPL_STM'};

if ($preset_stm eq "gtm") {
    $libatomics=$ENV{'CHPL_LIBATOMICS'};
} else {
    $libatomics="";
}

print "$libatomics\n";
exit(0);
