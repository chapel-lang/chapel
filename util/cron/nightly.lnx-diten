#!/usr/bin/env bash

source $(cd $(dirname $0) ; pwd)/common-gasnet.bash

# run cyclic dist regressions
export LOGFILE=$HOME/tmp/nightly.cyclic.out
log_info "Testing cyclic distribution.  Log file: $LOGFILE"
~/chapel/util/cron/nightly -cron -dist cyclic > $LOGFILE 2>&1

log_info "Finished ${0}."
