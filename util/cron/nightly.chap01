#!/bin/tcsh
echo `date` "" Starting $0 on `hostname`.

set LOGFILE = $HOME/tmp/nightly.linux64.out
echo `date` "" "Testing the default-everything configuration.  Log file: $LOGFILE"
source ~/.chapelcshrc-cron && ~/chapel/util/cron/nightly -cron >&! $LOGFILE

echo `date` "" Finished $0.
