#!/usr/bin/env bash

source $(cd $(dirname $0) ; pwd)/common.bash

export LOGFILE=$HOME/tmp/nightly-nolocal.out
log_info "Testing --no-local.  Log file: $LOGFILE"
~/chapel/util/cron/nightly -cron -no-local > $LOGFILE 2>&1

log_info "Finished ${0}."
