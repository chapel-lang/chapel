#!/usr/bin/env bash

source $(cd $(dirname $0) ; pwd)/common.bash

# (thomasvandoren, 2014-02-19) This box under maintenance.
export CHPL_NIGHTLY_CRON_RECIPIENT=chapel_build@cray.com

~/chapel/util/cron/nightly.verify -full
