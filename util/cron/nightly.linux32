#!/usr/bin/env bash

source $(cd $(dirname $0) ; pwd)/common.bash

export LOGFILE=~/tmp/nightly.linux32virt.out
log_info "Testing comm=none.  Log file: $LOGFILE"
with_logging $LOGFILE ~/chapel/util/cron/nightly -cron -examples

source $(cd $(dirname $0) ; pwd)/common-gasnet.bash

export LOGFILE=~/tmp/nightly.linux32virt-gasnet.out
log_info "Testing comm=gasnet.  Log file: $LOGFILE"
with_logging $LOGFILE $CHPL_HOME/util/cron/nightly -cron -examples

log_info "Finished ${0}."
