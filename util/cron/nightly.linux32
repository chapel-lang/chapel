#!/usr/bin/env bash

CWD=$(cd $(dirname $0) ; pwd)

source $CWD/common.bash

log_info "Testing comm=none."
$CWD/nightly -cron -examples

source $CWD/common-gasnet.bash

log_info "Testing comm=gasnet."
$CWD/nightly -cron -examples

log_info "Finished ${0}."
