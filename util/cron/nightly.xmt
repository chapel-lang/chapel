#!/bin/tcsh
echo Starting $0 on `hostname`.; module list; printenv; echo

set LOGFILE = $HOME/tmp/nightly.mta-sanity.out
echo "Testing XMT on `date`.  Log file: $LOGFILE"
~/chapel/util/cron/nightly-mta.cron >&! $LOGFILE

echo Finished $0 on `date`.
