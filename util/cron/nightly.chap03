#!/usr/bin/env bash

source $(cd $(dirname $0) ; pwd)/common.bash

export LOGFILE=$HOME/tmp/nightly-perf.linux64.chap03.out
log_info "Testing performance.  Log file: $LOGFILE"

export CHPL_NIGHTLY_LOGDIR=/data/sea/cascade/chapel/NightlyPerformance/chap03

~/chapel/util/cron/nightly -cron -performance -numtrials 5 -startdate 08/21/07 > $LOGFILE 2>&1

log_info "Finished ${0}."
