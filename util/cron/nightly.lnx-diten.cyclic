#!/usr/bin/env sh

. /etc/modules/sh
module use /opt/cpkg/current/modulefiles
module load subversion

export CHPL_COMM=gasnet
export GASNET_SPAWNFN=L
export GASNET_QUIET=yes

$NIGHTLY -cron -dist cyclic

