#!/usr/bin/env bash
. /etc/profile

rm -f /tmp/nightly.baseline.out
source ~chapelu/.chplcron.baseline.bashrc && /users/chapelu/chapel/util/cron/nightly -cron -baseline -suppress Suppressions/baseline.suppress >& /tmp/nightly.baseline.out

cp /tmp/nightly.baseline.out ~/tmp/nightly.baseline.out

