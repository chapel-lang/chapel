#!/bin/bash

if [ -n "${CHPL_HOME:-x}" ] ; then
    chapelDir="$CHPL_HOME"
else
    chapelDir="./"
fi

pushd $chapelDir > /dev/null

find modules -name \*.chpl -print0 | xargs -0 grep -H $@

popd > /dev/null
