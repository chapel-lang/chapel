#!/bin/csh

set patchdir=~/patches
set patchfile=$patchdir/$1.patch

cd $CHPL_HOME
svn diff --no-diff-deleted > $patchdir/auto_receive_patch.patch
svn revert --recursive .
svn update
patch -p0 < $patchfile
