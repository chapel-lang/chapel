cg-chpl-lineno

This needs to be cleaned up.  The info is currently hacked up during code generation.

It seems that when generating conditionals with
the --cg-chpl-lineno flag, both paths are taken.
