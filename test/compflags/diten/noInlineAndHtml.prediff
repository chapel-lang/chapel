#!/usr/bin/env bash

testname="$1"
outfile="$2"
tmpfile="$outfile.prediff.tmp"

mv $outfile $tmpfile
sed -e 's/MIS[0-9][0-9]*/MIS#/' $tmpfile | sed -e 's/Version .*$/Version #/' > $outfile
rm -rf $tmpfile log/
