// This wrapper lets us run the same code (testReplicatedVar2.chpl)
// on single and multiple locales with different .good files.

// This runs all top-level (aka "initialization") code from that file,
// so there is nothing for us to do here.
use testReplicatedVar2;
