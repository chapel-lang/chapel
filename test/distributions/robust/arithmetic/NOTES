types and parameters
X domain.rank and array.rank
X domain.idxType
X array.eltType

basics
X infer type of domain
X infer type of array
X domain assignment
X array assignment
X array assignment where RHS is a tuple
X array swap
X array accesses (reads and writes)

functions
X reshape on arrays
X write on domains and arrays

methods
X domain.dist
X array.domain
X domain.dim
X domain.dims
X domain.numIndices and array.numElements
X domain.member
X domain.expand
X domain.exterior
X domain.interior
X domain.translate
X domain +/- index

slicing
X domain and array slices
  local domain and array slices

rank change
X domain and array rank change
  local domain and array rank change

reindexing
X array reindexing via function calls

aliases
X array aliases with slicing and reindexing

striding
X strided domains and arrays
X   and slicing
X   and rank change
X   and reindexing
X   and aliases

iterating
X serial domain and array iteration
X parallel domain and array iteration

variable domains
X increasing domain size adds elements to arrays
X decreasing domain size removes elements from arrays

robustness
X build and destroy many domains and arrays
X iterate over domains and arrays many times

X distributed arrays of arrays
X arrays of distributed arrays
X distributed arrays of distributed arrays
X promotion via distributed arrays
X reductions over distributed arrays
X scans over distributed arrays
X classes with distributed array fields
X records with distributed array fields
