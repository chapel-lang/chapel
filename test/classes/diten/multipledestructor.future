error message: multiple destructors result in slightly weird error message

In particular, the last line is:

	<internal>:-1: note:                 object.~chpl_destroy()

Do we really need this history here?

Was:

This is now giving a decent error message:  ambiguous call of the two destructors.

Was:

bug: multiply defined destructor causes segfault in compiler
