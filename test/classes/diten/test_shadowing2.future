semantic: Accessing a shadowed field in the base class

This is attempting to shadow a field, then access the base version of it.
It currently fails due to a bug (see test_shadowing.future), but the spec
claims we haven't defined this yet.
