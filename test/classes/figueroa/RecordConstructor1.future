semantic: 0-arg constructor is not called as default

Should it be?

Was:

bug: function resolution fails when a user-defined constructor for a record present

The compiler emits an error message about an unresolved call if a record has a
user-defined constructor.
