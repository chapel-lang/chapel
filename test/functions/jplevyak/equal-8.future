deitz

var x = y;

sets noDefaultInit which means that if the
type is determined to be a value type (e.g. record)
then no default initialization is done which means
that x doesn't get it's own storage.  Perhaps
exprType should be set?

