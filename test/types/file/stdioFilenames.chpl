var  fin : file = stdin;
var fout : file = stdout; 
var ferr : file = stderr;

writeln("fin.filename  = ", fin.filename);
writeln("fout.filename = ", fout.filename);
writeln("ferr.filename = ", ferr.filename);
