var str1 = "abc";
var str2 = "abc";
var str3 = "abd";

writeln(str1, " == ", str2, " => ", (str1 == str2));
writeln(str1, " == ", str3, " => ", (str1 == str3));

writeln(str1, " != ", str2, " => ", (str1 != str2));
writeln(str1, " != ", str3, " => ", (str1 != str3));

writeln(str1, " <= ", str2, " => ", (str1 <= str2));
writeln(str1, " <= ", str3, " => ", (str1 <= str3));

writeln(str1, " < ", str2, " => ", (str1 < str2));
writeln(str1, " < ", str3, " => ", (str1 < str3));

writeln(str1, " >= ", str2, " => ", (str1 >= str2));
writeln(str1, " >= ", str3, " => ", (str1 >= str3));

writeln(str1, " > ", str2, " => ", (str1 > str2));
writeln(str1, " > ", str3, " => ", (str1 > str3));
