basic.chpl:30: warning: the type 'c_string' is deprecated; use the variant of 'createCopyingBuffer' that takes a 'c_ptrConst' instead
basic.chpl:31: warning: the type 'c_string' is deprecated; use the variant of 'createBorrowingBuffer' that takes a 'c_ptrConst' instead
basic.chpl:32: warning: the type 'c_string' is deprecated; use the variant of 'createAdoptingBuffer' that takes a 'c_ptrConst' instead
Initialize from string
A Chapel string
A local Chapel string

Initialize from c_string
ABC
ABC
ABC

Initialize from c_ptr(uint(8))
ABC
ABC
ABC
ABC
A C
A C

Initialize from c_ptr(c_char)
ABC
ABC
ABC
ABC
A C
A C

Initialize from c_ptrConst(c_uchar)
ABC
ABC
ABC
ABC
A C
A C
