# this test doesn't work on cygwin, probably due to the precomp file...
CHPL_TARGET_PLATFORM==cygwin64
CHPL_TARGET_PLATFORM==cygwin32
# this test works on --llvm but the .good file is trivially different
COMPOPTS <= --llvm
