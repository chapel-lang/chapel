─── error in multipleQuestionArgs.chpl:4 [MultipleQuestionArgs] ───
  Cannot have '?' more than once in a call
  The first ? argument occurs here:
      |
    4 | test(?, ?, ?);
      |      ⎺
      |
  The second ? argument occurs here:
      |
    4 | test(?, ?, ?);
      |         ⎺
      |

─── error in multipleQuestionArgs.chpl:4 [MultipleQuestionArgs] ───
  Cannot have '?' more than once in a call
  The first ? argument occurs here:
      |
    4 | test(?, ?, ?);
      |      ⎺
      |
  The second ? argument occurs here:
      |
    4 | test(?, ?, ?);
      |            ⎺
      |

─── error in multipleQuestionArgs.chpl:4 [NoMatchingCandidates] ───
  Unable to resolve call to 'test': no matching candidates.
      |
    4 | test(?, ?, ?);
      |
  
  The following candidate didn't match because it expects 3 actuals, but none were provided.
      |
    1 | proc test(a, b, c) {
      |

