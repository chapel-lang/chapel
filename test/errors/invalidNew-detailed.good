─── error in invalidNew.chpl:3 [MemManagementNonClass] ───
  Cannot use memory management strategy 'owned' with non-class type 'E'.
      |
    3 | var y = new owned E();
      |                   ⎺    
      |
  Memory management strategies can only be used with classes.

─── error in invalidNew.chpl:3 [InvalidNew] ───
  Invalid use of 'new' with type 'E', which is neither a class nor a record.
      |
    3 | var y = new owned E();
      |                   ⎺    
      |
  The 'new' expression can only be used with records or classes.

