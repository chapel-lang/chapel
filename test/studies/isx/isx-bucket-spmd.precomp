#!/usr/bin/env bash
rm -f *.good
if [ "gasnet" = `$CHPL_HOME/util/chplenv/chpl_comm.py` ]; then cp goods-bucket-4/*.good .; else cp goods-bucket-1/*.good .; fi
sed -i .bak s/locale/bucket/ *.good
sed -i .bak s/Locale/Bucket/ *.good
rm *.bak
