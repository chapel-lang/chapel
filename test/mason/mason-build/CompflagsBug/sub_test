#!/usr/bin/env bash

source functions.bash

# this is a make-shift skipif mechanism

mason_exec=$(dirname $1)/mason
mason_ver=$(${mason_exec} --version)


if [ $? -eq 0 ]; then

  mason_test_start $1

  test_command "mason build"
  test_command "mason run"

  mason_test_end $0
fi
