#!/bin/bash


skip="False"
zlibcall= $(pkg-config zlib --exists)
zlib=$?
if  [ $zlib = 0 ] ; then
    skip="False"
else
    skip="True"
fi

opensslCall= $(pkg-config openssl --exists)
openssl=$?

if [ $openssl = 0 ] ; then
    skip="False"
else
    skip="True"
fi

echo "$skip"
