Created new application project: newTest
Tip: To convert existing code to a mason project, move the driver application to the `src/newTest.chpl` file. For adding other source code, using submodules is the recommended approach to avoid namespace collisions.
[brick]
name="newTest"
version="0.1.0"
chplVersion="CHPL_CUR_FULL"
license="None"
type="application"

[dependencies]
