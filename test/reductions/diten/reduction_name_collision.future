bug: reductions involving variables named "r" can produce incorrect results.

There is a variable called "r" in the ChapelReduce._reduce function that gets
passed to the next() method instead of the global variable r of type R. An
identical problem happens with scans.
