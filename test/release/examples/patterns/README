=========================
Chapel Examples: Patterns
=========================

This directory is designed to store sample programs that demonstrate
common patterns written in Chapel.

parallelIO.chpl : uses parallelism to write/read an array to/from a file
recordio.chpl  : uses formatted I/O to read/write records from/to files
