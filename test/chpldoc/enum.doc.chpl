/* This is a color enum. */
enum Color { Red, Yellow, Blue };


/* Another enum with inline comments for constants. */
enum Months {
  January = 1,
  /* month two! */ February,
  /* the ides of */ March,
  April,
  May,
  June,
  July,
  August,
  September,
  October,
  November,
  December
};
