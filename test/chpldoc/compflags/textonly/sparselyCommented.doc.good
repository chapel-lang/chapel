sparselyCommented.doc.chpl:36: warning: This file-scope code is outside of any explicit module declarations (e.g., module Other), so an implicit module named 'sparselyCommented.doc' is being introduced to contain the file's contents.
sparselyCommented.doc

Usage:
   use sparselyCommented.doc;

or

   import sparselyCommented.doc;

Submodules for this module are located in the sparselyCommented.doc/ directory

   proc method1()
      Comment for method1 

   proc method1Commentless()
   proc body()
      Comment for method with a body 

   proc bodyCommentless()
   proc hasArg(val: int)
      Comment for method with an argument 

   proc hasArgCommentless(val: int)
   proc argAndBody(val: int)
      Comment for method with argument and body 

   proc argAndBodyCommentless(val: int)
Other

Usage:
   use sparselyCommented.doc.Other;

or

   import sparselyCommented.doc.Other;

   proc inside()
      Comment for method inside module 

   proc insideCommentless()
   proc body()
      Comment for method with a body inside module 

   proc bodyCommentless()
   proc hasArg(val: int)
      Comment for method with an argument inside module 

   proc hasArgCommentless(val: int)
   proc argAndBody(val: int)
      Comment for method with argument and body inside module 

   proc argAndBodyCommentless(val: int)
