record Foo {
  var aComplex: complex = 1.0 + 2.0i;
  const bar = 0.0 + 1.0i;
  param baz: complex;

  /* This one has a comment */
  var aComplex2: complex = 1.0 + 2.0i;
  /* So does this one */
  const bar2 = 0.0 + 1.0i;
  /* And this one */
  param baz2: complex;
}
