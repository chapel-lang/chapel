
FLAGS:
======
  -h                    -- print this message

CONFIG VAR FLAGS:
=================
  -s<configVar>=<value> -- set the value of a config var
  -f<filename>          -- read in a file of config var assignments

MEMORY FLAGS:
=============
  --memmax=<n>          -- simulate 'n' bytes of memory available
  --memtrace=<filename> -- write memory trace to filename
  --memthreshold=<n>    -- filter memtrace for sizes >= 'n' bytes

CONFIG VARS:
============
module1's config vars:
  x: this is a test (default:  that seems to work)

module2's config vars:
  x: 888 (default:  2)
  z: no module name given (default:  this one doesn't require it)
