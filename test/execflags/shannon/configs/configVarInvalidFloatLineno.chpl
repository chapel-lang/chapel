config var x: real = 1.5;
config var y: int = 1;
writeln("x is initialized at compile time to 1.5");
writeln("x at runtime is ", x);
writeln("y is initialized at compile time to 1");
writeln("y at runtime is ", y); 
