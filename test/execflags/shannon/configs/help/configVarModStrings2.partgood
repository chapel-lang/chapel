module1 config vars:
                       x: string (configured to this is a test)

module2 config vars:
                       x: string (configured to of setting strings in two modules)
                       z: string
