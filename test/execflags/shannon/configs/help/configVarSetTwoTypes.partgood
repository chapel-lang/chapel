module1 config vars:
                       x: int(64) (configured to 9)

module2 config vars:
                       x: real(64) (configured to 8.888)
                       z: string (configured to string set without module name)
