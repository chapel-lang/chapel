module1 config vars:
                       x: int(64) (configured to 9)

module2 config vars:
                       x: int(64) (configured to 88)
                       z: real(64) (configured to 8.8)
