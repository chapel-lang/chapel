config var x: bool = true;
config var y: bool = false;

writeln("x is initialized at compile time to true");
writeln("x at runtime is ", x);
writeln("y is initialized at compile time to false");
writeln("y at runtime is ", y);
