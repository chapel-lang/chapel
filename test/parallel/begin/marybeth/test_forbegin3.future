bug: op= on sync vars

The assignment:  count += 1, where count is a sync var, gets the
following compiler error:

test_forbegin3.chpl:16: error: unresolved call 'readFE(_syncvar(int))'

The assignment: count = count + 1, where count is a sync var does not
get this error.

