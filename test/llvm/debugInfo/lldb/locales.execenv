#!/usr/bin/env bash

exec_name=$1
compopts=$4

if [[ "$CHPL_COMM" != 'none' || "$COMPOPTS" == *'--no-local'* ]]; then
  echo "CHPL_RT_DEBUGGER_CMD_FILE=${exec_name}NoLocalCommands.txt"
else
  echo "CHPL_RT_DEBUGGER_CMD_FILE=${exec_name}Commands.txt"
fi
