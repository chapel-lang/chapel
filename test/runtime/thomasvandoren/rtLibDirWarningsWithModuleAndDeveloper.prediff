#!/usr/bin/env bash
#
# Run the compiler with CHPL_DEVELOPER=true, CHPL_MODULE_HOME == CHPL_HOME, and
# opposite hwloc value.

export CHPL_DEVELOPER=true
export CHPL_MODULE_HOME=$CHPL_HOME
./rerun_compiler_with_opposite_hwloc.bash $@
