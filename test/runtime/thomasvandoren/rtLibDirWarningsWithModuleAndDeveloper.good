$CHPL_HOME/runtime/etc/Makefile.include:nnnnn: Expected runtime configuration: <runtime_config>
$CHPL_HOME/runtime/etc/Makefile.include:nnnnn: *** The requested configuration is not included in the module. Please send the package maintainer the output of $CHPL_HOME/util/printchplenv and request support for this configuration.  Stop.
