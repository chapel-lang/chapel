use IO;

var x: int;
var y: string;
var z: bool;

write("Type an integer: ");
readln(x);
write("Type a string: ");
readln(y);
write("Type a boolean: ");
readln(z);

writeln("\n(x,y,z) is: ", (x,y,z));
