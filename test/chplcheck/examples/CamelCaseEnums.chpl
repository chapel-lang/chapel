/*
  Enums should use camelCase naming convention.
*/

// This enum uses PascalCase which violates the rule
enum MyEnum {
  MyValue1,
  MyValue2
}

// This enum uses camelCase which is correct
enum myEnum {
  myValue1,
  myValue2
}
