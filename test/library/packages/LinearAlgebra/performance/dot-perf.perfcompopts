#!/usr/bin/env bash
system=$($CHPL_HOME/util/printchplenv | grep 'CHPL_TARGET_PLATFORM' | cut -d\  -f2)

if [ "$system" = "cray-cs" ] || [ "$system" = "cray-xc" ]
then 
    echo "-s blasImpl=off"
    echo "-s blasImpl=blas"
else
    echo "-s blasImpl=off"
fi
