[package]
name = "sort-random-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8.4", features = ["small_rng"] }
rayon = "1.8.1"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"
