#!/usr/bin/env python

from distutils.spawn import find_executable

if find_executable("protoc") is not None and find_executable("protoc-gen-chpl") is not None:
    print(False)
else:
    print(True)
