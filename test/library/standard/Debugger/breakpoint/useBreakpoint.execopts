#!/usr/bin/env bash

if command -v lldb >/dev/null ; then
  echo "--lldb"
elif command -v gdb >/dev/null ; then
  echo "--gdb"
else
  # this skipif should prevent this case from running
  echo "error: No debugger found" >&2
  exit 1
fi
