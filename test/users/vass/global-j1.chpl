//== __finite
config var __finite, __finitef, __finitel: int;
writeln(__finite, __finitef, __finitel);

//== __fpclassify
config var __fpclassify, __fpclassifyf, __fpclassifyl: int;
writeln(__fpclassify, __fpclassifyf, __fpclassifyl);

//== __isinf
config var __isinf, __isinff, __isinfl: int;
writeln(__isinf, __isinff, __isinfl);

//== __isnan
config var __isnan, __isnanf, __isnanl: int;
writeln(__isnan, __isnanf, __isnanl);

//== __signbit
config var __signbit, __signbitf, __signbitl: int;
writeln(__signbit, __signbitf, __signbitl);

//== acos
config var acos, acosf, acosl: int;
writeln(acos, acosf, acosl);

//== acosh
config var acosh, acoshf, acoshl: int;
writeln(acosh, acoshf, acoshl);

//== asin
config var asin, asinf, asinl: int;
writeln(asin, asinf, asinl);

//== asinh
config var asinh, asinhf, asinhl: int;
writeln(asinh, asinhf, asinhl);

//== atan
config var atan, atanf, atanl: int;
writeln(atan, atanf, atanl);

//== atan2
config var atan2, atan2f, atan2l: int;
writeln(atan2, atan2f, atan2l);

//== atanh
config var atanh, atanhf, atanhl: int;
writeln(atanh, atanhf, atanhl);

//== cbrt
config var cbrt, cbrtf, cbrtl: int;
writeln(cbrt, cbrtf, cbrtl);

//== ceil
config var ceil, ceilf, ceill: int;
writeln(ceil, ceilf, ceill);

//== copysign
config var copysign, copysignf, copysignl: int;
writeln(copysign, copysignf, copysignl);

//== cos
config var cos, cosf, cosl: int;
writeln(cos, cosf, cosl);

//== cosh
config var cosh, coshf, coshl: int;
writeln(cosh, coshf, coshl);

//== drem
config var drem, dremf, dreml: int;
writeln(drem, dremf, dreml);

//== erf
config var erf, erff, erfl: int;
writeln(erf, erff, erfl);

//== erfc
config var erfc, erfcf, erfcl: int;
writeln(erfc, erfcf, erfcl);

//== exp
config var exp, expf, expl: int;
writeln(exp, expf, expl);

//== exp10
config var exp10, exp10f, exp10l: int;
writeln(exp10, exp10f, exp10l);

//== exp2
config var exp2, exp2f, exp2l: int;
writeln(exp2, exp2f, exp2l);

//== expm1
config var expm1, expm1f, expm1l: int;
writeln(expm1, expm1f, expm1l);

//== fabs
config var fabs, fabsf, fabsl: int;
writeln(fabs, fabsf, fabsl);

//== fdim
config var fdim, fdimf, fdiml: int;
writeln(fdim, fdimf, fdiml);

//== finite
config var finite, finitef, finitel: int;
writeln(finite, finitef, finitel);

//== floor
config var floor, floorf, floorl: int;
writeln(floor, floorf, floorl);

//== fma
config var fma, fmaf, fmal: int;
writeln(fma, fmaf, fmal);

//== fmax
config var fmax, fmaxf, fmaxl: int;
writeln(fmax, fmaxf, fmaxl);

//== fmin
config var fmin, fminf, fminl: int;
writeln(fmin, fminf, fminl);

//== fmod
config var fmod, fmodf, fmodl: int;
writeln(fmod, fmodf, fmodl);

//== frexp
config var frexp, frexpf, frexpl: int;
writeln(frexp, frexpf, frexpl);

//== gamma
config var gamma, gammaf, gammal: int;
writeln(gamma, gammaf, gammal);

//== hypot
config var hypot, hypotf, hypotl: int;
writeln(hypot, hypotf, hypotl);

//== ilogb
config var ilogb, ilogbf, ilogbl: int;
writeln(ilogb, ilogbf, ilogbl);

//== isinf
config var isinf, isinff, isinfl: int;
writeln(isinf, isinff, isinfl);

//== isnan
config var isnan, isnanf, isnanl: int;
writeln(isnan, isnanf, isnanl);

//== j0
config var j0, j0f, j0l: int;
writeln(j0, j0f, j0l);

//== j1
config var j1, j1f, j1l: int;
writeln(j1, j1f, j1l);

//== jn
config var jn, jnf, jnl: int;
writeln(jn, jnf, jnl);

//== ldexp
config var ldexp, ldexpf, ldexpl: int;
writeln(ldexp, ldexpf, ldexpl);

//== lgamma
config var lgamma, lgammaf, lgammal: int;
writeln(lgamma, lgammaf, lgammal);

//== llrint
config var llrint, llrintf, llrintl: int;
writeln(llrint, llrintf, llrintl);

//== llround
config var llround, llroundf, llroundl: int;
writeln(llround, llroundf, llroundl);

//== log
config var log, logf, logl: int;
writeln(log, logf, logl);

//== log10
config var log10, log10f, log10l: int;
writeln(log10, log10f, log10l);

//== log1p
config var log1p, log1pf, log1pl: int;
writeln(log1p, log1pf, log1pl);

//== log2
config var log2, log2f, log2l: int;
writeln(log2, log2f, log2l);

//== logb
config var logb, logbf, logbl: int;
writeln(logb, logbf, logbl);

//== lrint
config var lrint, lrintf, lrintl: int;
writeln(lrint, lrintf, lrintl);

//== lround
config var lround, lroundf, lroundl: int;
writeln(lround, lroundf, lroundl);

//== modf
config var modf, modff, modfl: int;
writeln(modf, modff, modfl);

//== nan
config var nan, nanf, nanl: int;
writeln(nan, nanf, nanl);

//== nearbyint
config var nearbyint, nearbyintf, nearbyintl: int;
writeln(nearbyint, nearbyintf, nearbyintl);

//== nextafter
config var nextafter, nextafterf, nextafterl: int;
writeln(nextafter, nextafterf, nextafterl);

//== nexttoward
config var nexttoward, nexttowardf, nexttowardl: int;
writeln(nexttoward, nexttowardf, nexttowardl);

//== pow
config var pow, powf, powl: int;
writeln(pow, powf, powl);

//== pow10
config var pow10, pow10f, pow10l: int;
writeln(pow10, pow10f, pow10l);

//== remainder
config var remainder, remainderf, remainderl: int;
writeln(remainder, remainderf, remainderl);

//== remquo
config var remquo, remquof, remquol: int;
writeln(remquo, remquof, remquol);

//== rint
config var rint, rintf, rintl: int;
writeln(rint, rintf, rintl);

//== round
config var round, roundf, roundl: int;
writeln(round, roundf, roundl);

//== scalb
config var scalb, scalbf, scalbl: int;
writeln(scalb, scalbf, scalbl);

//== scalbln
config var scalbln, scalblnf, scalblnl: int;
writeln(scalbln, scalblnf, scalblnl);

//== scalbn
config var scalbn, scalbnf, scalbnl: int;
writeln(scalbn, scalbnf, scalbnl);

//== significand
config var significand, significandf, significandl: int;
writeln(significand, significandf, significandl);

//== sin
config var sin, sinf, sinl: int;
writeln(sin, sinf, sinl);

//== sincos
config var sincos, sincosf, sincosl: int;
writeln(sincos, sincosf, sincosl);

//== sinh
config var sinh, sinhf, sinhl: int;
writeln(sinh, sinhf, sinhl);

//== sqrt
config var sqrt, sqrtf, sqrtl: int;
writeln(sqrt, sqrtf, sqrtl);

//== tan
config var tan, tanf, tanl: int;
writeln(tan, tanf, tanl);

//== tanh
config var tanh, tanhf, tanhl: int;
writeln(tanh, tanhf, tanhl);

//== tgamma
config var tgamma, tgammaf, tgammal: int;
writeln(tgamma, tgammaf, tgammal);

//== trunc
config var trunc, truncf, truncl: int;
writeln(trunc, truncf, truncl);

//== y0
config var y0, y0f, y0l: int;
writeln(y0, y0f, y0l);

//== y1
config var y1, y1f, y1l: int;
writeln(y1, y1f, y1l);

//== yn
config var yn, ynf, ynl: int;
writeln(yn, ynf, ynl);

