bug: For some reason this test gets SIGFPE sometimes when exiting.

I've only seen this fail with gasnet for multiple locales.  It doesn't
do it all the time, and it more reliably happens when running within
the test system.

