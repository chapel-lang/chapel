#!/bin/bash
outfile=$2

mv $outfile $outfile.tmp

# grep for specific items in valgrind output
grep -l 'Invalid read of size' $outfile.tmp > $outfile

# also grep for the desired output
grep ^2 $outfile.tmp >> $outfile

rm $outfile.tmp
