I'd expect to see some sort of error about indexing into a
2D array with only a single index in this case, if that is
possible.  If we think this is incorrect, I'd like to
reconsider this decision in the language.

