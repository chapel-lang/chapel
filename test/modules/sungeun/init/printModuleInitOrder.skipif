# Dead module elimination that is run with deadCodeElimination
# removes some modules, meaning that they no longer get initialized.
# This skips baseline testing so that we only test the
# initialization output when those modules are eliminated.
COMPOPTS <= --baseline
COMPOPTS <= --no-inline
