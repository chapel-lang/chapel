bug: can't call method of class defined in different module

Function in one module uses another module and returns a class defined in that
module. Methods from the returned class aren't found because the call site
didn't use the other module.
