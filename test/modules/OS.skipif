# This module should be insensitive to the Chapel configuration, so it
# should be sufficient to test it only in the standard one.
CHPL_COMM != none
CHPL_TARGET_MEM != jemalloc
CHPL_TASKS != qthreads
COMPOPTS <= --baseline
COMPOPTS <= --no-local
CHPL_TEST_PERF == on
CHPL_TEST_VGRND_EXE == on
