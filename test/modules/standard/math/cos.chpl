var pi : float = 3.1415926535;

writeln("pi = ", pi);

writeln("cos(-2*pi) = ", cos(-2*pi));
writeln("cos(-3*pi/2) = ", cos(-3*pi/2));
writeln("cos(-pi) = ", cos(-pi));
writeln("cos(-pi/2) = ", cos(-pi/2));

-- BRAD
-- Note:  I get an error in the interpreter if I take cos(0).
writeln("cos(0.0) = ", cos(0.0));

writeln("cos(pi/2) = ", cos(pi/2));
writeln("cos(pi) = ", cos(pi));
writeln("cos(3*pi/2) = ", cos(3*pi/2));
writeln("cos(2*pi) = ", cos(2*pi));
