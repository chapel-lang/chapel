The tests in this directory were set aside because 
they don't work on all platforms.  They are interpreted
instead of compiled and executed because the interpreter 
error message is not user dependent.  

The following math functions are not part of the sunos
math library:

exp2()
log2()
nearbyint()
round()
tgamma()
trunc()
