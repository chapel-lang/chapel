#!/bin/bash

# Note: I tried using a cleanfiles for this test, but it was failing to
# clean up the links.  So instead, I have the prediff clean them up, since
# they aren't needed.  I'm sorry

if [ -e symlinkToRoot ]; then
    rm symlinkToRoot
fi
if [ -e notAMonkey/symlinkToRoot ]; then
    rm notAMonkey/symlinkToRoot
fi
