var x = 3;

// eventually, we want to make this actually use and call the other
// modules

use ModA;
use ModB;
use ModC;
use ModD;
use ModE;
use ModF;

proc main() {
  writeln("x is: ", x);
  runa();
  runb();
  runc();
  rund();
  runea();
  runeb();
  runf();
}
