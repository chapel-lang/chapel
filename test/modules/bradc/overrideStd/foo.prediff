#!/bin/csh -f
set testname = $1
set outfile  = $2

set tmpfile = $outfile.prediff.tmp
mv $outfile $tmpfile
sed 's,[-0-9a-zA-Z_/\.]*modules/standard,$CHPL_HOME/modules/standard,g' $tmpfile > $outfile
rm $tmpfile
