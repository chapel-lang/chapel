# IMPORTANT NOTE: in the first line below, replace REPLACE_ME_FIRST
# with the location of your installation

export CHPL_HOME=REPLACE_ME_FIRST/chapel   

export CHPL_HOST_PLATFORM=`$CHPL_HOME/util/platform.pl`

export PATH=$PATH:$CHPL_HOME/bin/$CHPL_HOST_PLATFORM
