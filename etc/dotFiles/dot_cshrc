# IMPORTANT NOTE: in the first line below, replace REPLACE_ME_FIRST
# with the location of your installation

setenv CHPLHOME REPLACE_ME_FIRST/chapel   

setenv CHPL_PLATFORM `$CHPLHOME/util/platform`

set path = ($CHPLHOME/bin/$CHPL_PLATFORM $path)
