==========================
Qthreads README for Chapel
==========================

Local modifications:

* Added chpl_task_createCommTask() in place of
  chpl_task_perPthreadInit() to reflect interface change on Chapel's
  part.

* Changed the default callStackSize to 32k * sizeof(size_t) since most
  Chapel tasks are longer-lived than qthreads' default.