name: email
on:
    push:
    pull_request:
     
      
jobs: 
  send_email:
   # run this workflow step only when the PR is merged 
    if:  github.event.pull_request.merged == true 
    runs-on: ubuntu-latest 
    # use the github environment secrets 
    environment: Email
    env:
     SERVER: ${{secrets.EMAIL_SERVER}}
     EMAIL: ${{secrets.EMAIL_USER}}
     PASSWORD: ${{secrets.EMAIL_PASSWORD}}
    steps:
      - name: print secrets
        run:  |
         echo  ${{env.SERVER}}     
       
             
      