Unimplemented feature list

HIGH PRIORITY ITEMS (For implementation before Dec 1)

DONE int/uint semantics "var x: uint = 12" "var y: int = 12u"
DONE byte width semantics  "var x : int(8) = 2" where 2 defaults to int(64)
DONE floating point literals
DONE variable declarations with shared types
     constructors
DONE nested type aliases
DONE sequence # operator
DONE sequence indexing with negative integers
DONE sequence indexing by a tuple
DONE cross product function application
DONE sequence indexing with a sequence
DONE predicate expressions "x = [i in D] if i < 10 then ..."
     iterator functions
DONE reductions
     indefinite domains
       -=/remove method
       member? method
DONE domain arguments to functions
DONE association of arrays to domains
DONE   domain assignment
DONE   domain reallocation
DONE   array reallocation
DONE array arguments to functions
DONE   with explicit domains
DONE   with queried domains
     scalar promotion to preserve arrayness
     single variables
     sync variables

MEDIUM PRIORITY ITEMS (Maybe implemented by Dec 1)

     primitive to string casts
     string to primitive functions
     config parameters
     config variables (more orthogonal implementation)
     underscore to destructure tuples
     variables accessed via modules (mod.x)
     using modules not in files passed to the compiler
DONE unions
     indefinite sequences
       usage in zippered context
       usage in sequence indexing
     tuple to sequence cast
     sequence to tuple cast
     shadowing functions
     index types in domain type
     subdomains
     association of subdomains to domains
     array aliasing
     complex array initialization "var x : [i in D] int = i"
     arrays of arrays
     serial statement

LOW PRIORITY ITEMS (Probably not implemented by Dec 1)

     fixed size strings
     intents (in, out, inout, const)
     functions as arguments to functions
     conditional declarations
     module initialize function
     module finalize function
     predefined argv sequence
     anonymous record literals
     nested classes
     nested records
     sequence primitives
       reverse, spread, transpose, reshape, fill
     type select statements and wildcard underscore
     type variables nested in formal types "x : C(?t)"
     enumerated domains
     opaque domains
     sparse domains
     forall statement in parallel

CHOPPING BLOCK ITEMS

     static methods
     assignment of records to classes
     sequence of length-1 strings special features
     check for invalid goto usage
     any and all reductions
     implicit all reduction
     special interpretation of sequences in select statements
     cursors
     arithmetic index sets
     set_range method on arithmetic domains
     product domains
     array functions
